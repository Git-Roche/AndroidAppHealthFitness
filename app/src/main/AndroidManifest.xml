<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.phitboi2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".DietActivity"></activity>
        <activity android:name=".AddNewMealActivity"></activity>
        <activity android:name=".ExistingMealActivity"></activity>
        <activity android:name=".SetGoalPctActivity"></activity>
        <activity android:name=".LookupFoodActivity"></activity>

        <activity android:name=".ExerciseActivity"></activity>
        <activity android:name=".NewWorkoutActivity"></activity>
        <activity android:name=".NewExerciseListActivity"></activity>
        <activity android:name=".NewExerciseActivity"></activity>
        <activity android:name=".DoWorkoutActivity"></activity>
        <activity android:name=".OldWorkoutActivity"></activity>


    </application>
</manifest>